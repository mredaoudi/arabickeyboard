name: arabic_keyboard
description: "A Flutter Desktop App based on Lexilogos Keyboard."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.0-11.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  undo: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
